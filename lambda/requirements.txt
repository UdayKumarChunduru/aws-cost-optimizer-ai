# Lambda-specific dependencies only.
# boto3 and botocore are NOT listed here because the AWS Lambda Python
# runtime already includes them pre-installed - bundling them again
# wastes deployment package size for zero benefit.
#
# This file exists separately from the root requirements.txt because
# that file also includes fastapi, uvicorn, pytest, and moto, which
# Lambda never executes and would bloat the deployment package from
# under 1MB to 180+MB for no reason.
#
# Dependencies bundled into the Lambda deployment package.
# boto3 and botocore are excluded — the Lambda Python runtime
# provides them pre-installed. Everything else must be explicitly
# bundled or Lambda will fail with ImportModuleError.
pydantic==2.7.4
pydantic-settings==2.3.4
requests==2.32.3
